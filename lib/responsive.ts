import type { ViewStyle } from 'react-native';

export const MAX_CONTENT_WIDTH = 720;

export const tabletContainerStyle: ViewStyle = {
  width: '100%',
  maxWidth: MAX_CONTENT_WIDTH,
  alignSelf: 'center',
};
